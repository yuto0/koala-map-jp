# CLAUDE.md

## ファイル読み込みの方針

- **ファイルを指定されていない場合は、まず Glob や Grep で構造・箇所を特定してから、必要なファイルのみ Read する**
- コードベース全体を一括で読み込まない。目的のある読み込みのみ行う
- 「どのファイルに何があるか」を調べる段階では Read を使わず Glob/Grep を優先する

## コミュニケーション

- エラーを修正した場合、なぜそのエラーが発生したのか理由を明確に説明すること

## トラブルシューティング時にユーザーへ提案すること

エラーが長引く場合や堂々巡りになりそうな場合は、以下の情報を共有するよう提案する：

1. **エラーの全文をコマンドの実行から貼る**（途中からではなく最初から）
2. **直前に行った操作を伝える**（「○○を変更した後から壊れた」など）
3. **関連ファイルをすべて共有する**（`.dockerignore` などの隠しファイルも含む）
4. **「動かない」より「何をしたら何が起きた」を伝える**（コマンド → 結果の形式で）
