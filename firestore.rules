rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Client SDKs from browsers/mobile apps are blocked.
      // This project serves Firestore through the Go API server.
      allow read, write: if false;
    }
  }
}
